model Role {
    id          String           @id
    name        String           @unique
    permissions RolePermission[]
    createdAt   DateTime
    users       User[]
    updatedAt   DateTime?
    deletedAt   DateTime?
}
