model Role {
    id          String           @id
    name        String           @unique
    permissions RolePermission[]
    createdAt   DateTime
    updatedAt   DateTime?
    deletedAt   DateTime?
}
